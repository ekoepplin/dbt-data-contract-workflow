name: newsapi_demo
version: 0.1.0
config-version: 2

profile: newsapi_demo

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  newsapi_demo:
    staging:
      +materialized: table
